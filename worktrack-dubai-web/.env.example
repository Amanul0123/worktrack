# Local dev — Vite proxy handles /api → localhost:4000, so leave this blank
VITE_API_URL=

# Production (Vercel) — set this to your Render backend URL:
# VITE_API_URL=https://your-api.onrender.com/api
